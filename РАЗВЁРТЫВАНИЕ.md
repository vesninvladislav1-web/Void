# Что нужно сделать на сервере

Обычное обновление:

```
ssh root@155.212.164.11
cd /var/www/voidm.site && git pull && pm2 restart void-signal
```

Перезапуск обязателен всегда, когда менялся `server/server.js`.

---

## Один раз: адреса со ссылками

**Проверено 3 сентября 2026: на voidm.site это уже настроено.** Раздел
оставлен на случай переезда на другой сервер или новый домен.

Ссылки-приглашения (`/i/<код>`) и переписка без следа (`/t/<код>`) — это не
файлы на диске. Файла с таким именем нет, и nginx по умолчанию отвечает на них
`404 Not Found`. Человек, перешедший по ссылке, увидит страницу ошибки, а не
Void.

Лечится одной строкой в конфиге сайта. Открыть:

```
nano /etc/nginx/sites-available/voidm.site
```

Найти блок `location /` и привести его к такому виду:

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

`try_files` читается так: «поищи файл с таким именем; не нашёл — поищи папку;
не нашёл и её — отдай index.html». Приложение само разберёт адрес и покажет
нужное. Блоки `/api` и `/signal` трогать не надо, они идут выше и проверяются
раньше.

Проверить и применить:

```
nginx -t && systemctl reload nginx
```

`nginx -t` разбирает конфиг, ничего не меняя. Пока он не сказал `syntax is ok`
и `test is successful`, перезагружать нельзя: с испорченным конфигом nginx не
поднимется, и сайт ляжет целиком.

Проверка, что получилось:

```
curl -s -o /dev/null -w "%{http_code}\n" https://voidm.site/t/00112233445566778899aabbccddeeff
```

Должно быть `200`. Если `404` — конфиг не применился.

---

## Папка для картинок (сторонний проект)

На той же машине можно раздавать файлы по ссылке — например, чтобы показать
фотографию тому, кому её некуда приложить. Это никак не связано с Void и
намеренно лежит **вне** его папки: иначе `git pull` спотыкался бы о чужие
файлы, а они попадали бы в репозиторий.

```
mkdir -p /var/www/foto
chown -R www-data:www-data /var/www/foto
```

В конфиг сайта, **выше** блока `location /`:

```nginx
location /f/ {
    alias /var/www/foto/;
    autoindex off;          # список файлов наружу не отдаём
    try_files $uri =404;    # нет файла — честная 404, а не страница Void
    expires 7d;
}
```

Порядок важен. Ниже стоит `try_files $uri $uri/ /index.html`, и без
отдельного блока запрос за несуществующей картинкой вернул бы страницу Void
с кодом 200 — выглядело бы так, будто файл есть, но «не открывается».

Проверить и применить:

```
nginx -t && systemctl reload nginx
```

Что важно понимать про эту папку:

- **Всё в ней доступно любому, у кого есть ссылка.** Пароля там нет и быть не
  может: тот, кто по ссылке ходит, никакого пароля не знает. Защита только
  одна — длинное случайное имя файла, которое не подобрать.
- Поэтому туда не кладут документы, переписку и вообще ничего, что жалко.
- К обещаниям Void это не относится: там сквозное шифрование, здесь обычная
  раздача файлов. Это разные вещи на одной машине.
- Диск общий с Void. Следи за размером:
  `du -sh /var/www/foto` и `df -h`.

Удалить всё, что старше месяца:

```
find /var/www/foto -type f -mtime +30 -delete
```

---

## Присмотр за сервером

### Первое и самое важное: поднимется ли Void после перезагрузки VPS

Проверь **до всего остального** — это самая большая дыра из возможных:

```
pm2 startup
pm2 save
systemctl is-enabled pm2-root
```

Если `pm2 startup` печатает команду — выполни её. Если `is-enabled` отвечает
`enabled`, всё в порядке. Если нет — **при любой перезагрузке сервера Void
лежит, пока ты случайно не заметишь.**

### Проверка живости

```
curl -s https://voidm.site/health
```

Ответ `{"ok":true}` и код 200 — работает. Код 503 и `{"ok":false,"why":"..."}` —
не работает, и сказано почему: `db-write`, `db-read`, `disk`, `http`, `ws`.

Проверка **настоящая**: она пишет метку в базу и читает обратно. Прежний
`/api/status` отвечал «всё хорошо» даже с мёртвой базой, потому что запрос
к ней стоял в try/catch. Проверяется именно запись — при переполненном диске
чтение ещё идёт, а запись уже нет, и Void в этот момент не работает, хотя
выглядит живым.

Настоящая проверка делается не чаще раза в двадцать секунд, остальные запросы
получают прошлый ответ. Иначе достаточно было бы долбить `/health`, чтобы
нагрузить диск.

### Наблюдатель снаружи

Подключи UptimeRobot на `https://voidm.site/health`, **раз в пять минут**, и
свяжи с Telegram.

Почему снаружи, а не через сам Void: **мёртвый сервер не пришлёт уведомление
о том, что он мёртв.** Наблюдатель, живущий на том же VPS, умрёт вместе с ним.

Через Void ходят уведомления о том, что видно только изнутри и при живом
сервере: кончается место, срочная жалоба. Это уже работает.

Почему пять минут, а не одна: при нынешнем числе людей разница ничего не
стоит, а частые проверки дают ложные срабатывания на каждом чихе сети.
Уведомления, которые иногда врут, перестают читать — и пропускают настоящее.

### Пределы перезапуска

В репозитории лежит `ecosystem.config.js`. Он нужен затем, что сейчас
настройки pm2 живут только в памяти одной машины, и при переезде их пришлось
бы вспоминать.

Главное в нём — `max_restarts: 10` и `min_uptime: '60s'`. Без них так: битая
миграция базы, падение при старте, pm2 поднимает, снова падение — и по кругу.
Лог растёт, процессор занят, а процесс формально всё время «запускается», и
никто ничего не знает.

**Перед применением сохрани переменные окружения** — пароль администратора и
ключ разборщика жалоб живут в отдельном хранилище pm2:

```
pm2 get void-signal:VOID_ADMIN_PASSWORD
pm2 get void-signal:VOID_MOD_KEY
```

Выпиши что есть, потом:

```
cd /var/www/voidm.site
pm2 reload ecosystem.config.js --update-env
pm2 save
pm2 describe void-signal | grep -i restart
```

Если после этого вход перестанет работать — значит пароль не подхватился,
верни его командой `pm2 set void-signal:VOID_ADMIN_PASSWORD <пароль>` и
перезапусти.

### Отчего сервер падал

Причина падения теперь пишется в `server/padeniya.log` — время, сообщение,
место в коде и сколько сервер прожил до этого. Смотреть можно **с телефона**:
админ-панель → «Отчего падал сервер».

Раньше причина была только в логе pm2, а он перезаписывается, и добраться до
него можно лишь по ssh — то есть не тогда, когда нужно.

Sentry намеренно не подключали: это отправка ошибок вместе со стеками на
чужой сервер, а чужие серверы из Void мы как раз выносим.

---

## Шрифты лежат у нас

Папка `shrifty/` — 328 КБ, тринадцать файлов. Раньше буквы просились у
`fonts.googleapis.com`, и Google получал запись: чей адрес, когда и что человек
открыл voidm.site. Теперь наружу не уходит ничего.

**После первого обновления проверь тип, которым nginx отдаёт таблицу стилей:**

```
curl -sI https://voidm.site/shrifty/shrifty.css | grep -i content-type
```

Должно быть `text/css`. Если окажется `application/octet-stream` —
**браузер откажется применять стили молча**, и Void покажется системным
шрифтом без единой ошибки в консоли. На стенде я на это уже наступил: проверка
показывала «шрифты на месте», а на деле не грузился ни один.

Лечится тем, что в конфиге nginx есть строка `include mime.types;` — она обычно
на месте по умолчанию, но убедиться стоит один раз.

Шрифты под лицензией SIL Open Font License — раздавать со своего сервера можно.

---

## Резервные копии

Скрипт `server/backup.js` написан, но сам себя не запускает. Чтобы копии
делались каждую ночь, его надо отдать cron — расписанию, встроенному в Linux.

Сначала узнать, где лежит node (путь нужен полный: у cron нет твоих
привычных настроек оболочки):

```
which node
```

Затем открыть расписание и добавить строку:

```
crontab -e
```

```
0 4 * * * cd /var/www/voidm.site/server && /usr/bin/node backup.js /var/backups/void 7 >> /var/log/void-backup.log 2>&1
```

Разбор строки: `0 4 * * *` — минута 0, час 4, каждый день, каждый месяц, любой
день недели. То есть **каждую ночь в 4:00**. Дальше — что запустить. `7` в
конце: хранить семь последних копий, восьмая вытесняет первую.

Проверить, что записалось, и не дожидаясь ночи убедиться, что работает:

```
crontab -l
cd /var/www/voidm.site/server && node backup.js /var/backups/void 7
ls -la /var/backups/void
```

Наутро посмотреть, что прошло само:

```
tail -20 /var/log/void-backup.log
```

**Копия на той же машине спасает от ошибки, но не от смерти машины.** Раз в
неделю копию стоит уносить наружу — на второй сервер или к себе на компьютер:

```
scp -r root@155.212.164.11:/var/backups/void/<папка> .
```

---

## Сортировщик жалоб

Работает сразу и без всяких ключей: разбор идёт по списку слов. Никаких
действий он не совершает — только раскладывает по срочности и прячет мусор.

**Модель подключать необязательно.** Если хочется, чтобы разбор понимал
сарказм, цитату и ссору, а не только слова:

```
pm2 set void-signal:VOID_MOD_KEY sk-ant-...
pm2 restart void-signal
```

Необязательно и это:

```
pm2 set void-signal:VOID_MOD_MODEL claude-haiku-4-5-20251001
```

Проверить, что включилось — в логе при первой же жалобе:

```
pm2 logs void-signal --lines 50 | grep жалобы
```

Если модель недоступна или отвечает ошибкой, жалоба **всё равно принимается** и
разбирается правилами: в логе будет `[жалобы] Модель недоступна`. Потерять
обращение из-за чужого сервиса нельзя.

Ключ, как и пароль администратора, живёт только в pm2 — **в репозиторий его
класть нельзя**.

---

## Как отличить, кто ответил

Полезно помнить, потому что это экономит часы:

- **Void отвечает JSON** — `{"error":"..."}`;
- **nginx отвечает HTML** — страницей с заголовком вроде `413 Request Entity Too Large`.

Если в ответе HTML — до приложения запрос не дошёл, и чинить надо nginx.
